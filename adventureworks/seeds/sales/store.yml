version: 2

seeds:
  - name: store
    config:
      schema: sales
      column_types:
        business_entity_id: integer
        store_name: string
        sales_person_id: integer
        modified_date: timestamp
