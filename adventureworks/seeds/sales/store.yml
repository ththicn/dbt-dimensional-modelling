version: 2

seeds:
  - name: store
    config:
      schema: sales
      column_types:
        businessentityid: integer
        storename: string
        salespersonid: integer
        modifieddate: timestamp
