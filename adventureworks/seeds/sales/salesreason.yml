version: 2

seeds:
  - name: salesreason
    config:
      schema: sales
      column_types:
        salesreasonid: integer
        name: string
        reasontype: string
        modifieddate: timestamp
